name: first_ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  check:
    name: first_ci_check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv sync
      - run: uv run ruff check
      - run: uv run pytest