[project]
name = "product-holodex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jaikrishna Soundararajan", email = "jaikrishna096@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.1",
]

[build-system]
requires = ["uv_build>=0.11.21,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "matplotlib>=3.11.1",
    "pathlib>=1.0.1",
    "pre-commit>=4.6.1",
    "pytest>=9.1.1",
    "ruff>=0.16.0",
]
